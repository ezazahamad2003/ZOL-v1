// extract-call-details tests removed — extraction is now handled inside the
// intake-agent using Anthropic tool_use. See src/agents/intake-agent.ts.
import { describe, it } from 'vitest'
describe('extract-call-details (removed in MVP1)', () => {
  it('is intentionally empty', () => { /* noop */ })
})
