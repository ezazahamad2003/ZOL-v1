// calculate-quote tests removed — quotes feature dropped in MVP1.
// The orchestrator now uses query_calls, book_appointment, send_email etc.
// See tests for those tools instead.
import { describe, it } from 'vitest'
describe('calculate-quote (removed in MVP1)', () => {
  it('is intentionally empty', () => { /* noop */ })
})
